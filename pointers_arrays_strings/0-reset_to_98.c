#include "main.h"
#include <stdio.h>
/**
 * reset_to_98 - check the code
 *
 */
void reset_to_98(int *n)
{
	int i;
	int *p;
	i = *p;
	p = *n;
	for (p = 0; p <= *n; n++)
	{
		if (i = *n)
		{
			putchar(i);
		}
	putchar('\n');
	}
}
