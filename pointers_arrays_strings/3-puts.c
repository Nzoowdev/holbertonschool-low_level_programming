#include "main.h"
/**
 * _puts - Returns the length of a string
 * @str: Pointer to the string
 *
 * Return: Length of the string
 */
void _puts(char *str)
{
int e = 0;

for (e = 0; str[e] != '\0'; e++)
{
}
}
