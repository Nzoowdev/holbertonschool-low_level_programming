#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
{char ch = '0';
for (ch = '0'; ch <= '9'; ch++)
putchar(ch);
}
{char alph = 'a';
for (alph = 'a'; alph <= 'f'; alph++)
putchar(alph);
}
putchar('\n');
return (0);
}
