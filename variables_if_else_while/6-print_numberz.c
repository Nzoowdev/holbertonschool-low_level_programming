#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always à (Success)
 */
int main(void)
{
int alph = 0;
for (alph = '0'; alph <= '9' ; alph++)
{
putchar(alph);
}
putchar('\n');
return (0);
}
